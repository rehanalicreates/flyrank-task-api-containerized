# Copy this file to .env and fill in real values if they differ from these defaults.
# .env itself is gitignored and never committed — this file is the safe, public template.

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/flyrank_tasks
